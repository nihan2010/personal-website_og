{
  "name": "nihan-najeeb-portfolio",
  "version": "1.0.0",
  "description": "Ultra-premium liquid glass portfolio website for Nihan Najeeb P",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-deps": "npm install && cd server && npm install && cd client && npm install",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "keywords": ["portfolio", "fullstack", "react", "node", "liquid-glass", "ui-ux"],
  "author": "Nihan Najeeb P",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}